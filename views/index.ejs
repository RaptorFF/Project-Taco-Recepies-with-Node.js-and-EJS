<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
     <% if (!locals.tacoRecipe) { %>
    <h2>Pick your favourite taco ingredient 👆</h2>
    <% } else { %>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
     <h2><%= tacoRecipe.name %></h2>

    <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      <li><%= tacoRecipe.ingredients.protein.name %>, <%= tacoRecipe.ingredients.protein.preparation %></li>
      <li><%= tacoRecipe.ingredients.salsa.name %></li>
      <% for (i=0; i<tacoRecipe.ingredients.toppings.length; i++) { %>
      <li><%= tacoRecipe.ingredients.toppings[i].quantity %> of <%= tacoRecipe.ingredients.toppings[i].name %></li>
      <% } %>
    </ul>
    <% } %>



  </div>
</body>

</html>